<?xml version="1.0" encoding="UTF-8"?>
<ViewCell xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="sanitary.app.Views.EstimateViewCell">
  <ViewCell.View>
      <!--Ячейка для списка на странице скачать смету-->
        <StackLayout Orientation="Horizontal" Spacing="0">
            <Label Text="{Binding Description}" WidthRequest="145" TextColor="Black" FontSize="Default" Margin="5,0,0,0" VerticalOptions="Start"/>
            <Label TextColor="Black" FontSize="Default" Margin="5,0,35,0" VerticalOptions="Start">
                <Label.FormattedText>
                    <FormattedString>
                        <FormattedString.Spans>
                            <Span Text="{Binding Quantity}"/>
                            <Span Text=" шт"/>
                        </FormattedString.Spans>
                    </FormattedString>
                </Label.FormattedText>
            </Label>
            <Label Text="{Binding Price}" TextColor="Black" FontSize="Default" Margin="5,0,35,0" VerticalOptions="Start"/>
            <Label Text="{Binding Total}" TextColor="Black" FontSize="Default" VerticalOptions="Start"/>
      </StackLayout>
  </ViewCell.View>
</ViewCell>